/**
 * Temporary file to only render this as a single-city Scout application
 * until Multi-City is fully ready to launch.
 */
export const USE_SINGLE_CITY = false;
export const DISABLE_USER_ACCOUNTS = true;
